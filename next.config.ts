import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Empty config - Next.js 16 should work with dev tunnels by default
  // when using -H 0.0.0.0 flag
};

export default nextConfig;
